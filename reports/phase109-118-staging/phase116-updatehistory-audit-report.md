# Phase116: updateHistory.json 監査レポート（調査専用・編集なし）

作業日: 2026-08-24
対象: `src/data/updateHistory.json`（全124件）
方針: 本Phaseは調査・提案専用。`src/data/updateHistory.json`は直接編集していない。発見事項と修正案は本レポートおよび `phase116-updatehistory-audit-findings.json` に構造化して記録し、Phase118統合時の反映判断に委ねる。

## 実施した確認

1. `git log`直近30〜50コミットとの照合（`3c3af0b`〜`dbdc53a`を中心に、必要に応じて更に遡って確認）
2. 特に `6c561dc`（Phase89-98継続）・`dbdc53a`（Phase99-108）・`c0434c7`（トップページ数値不一致の原因究明+恒久監視化）の3コミットの差分を精読し、市民向け変更の掲載漏れがないか確認
3. 配列の並び順（日付順）を機械チェックし、既知のQAレポート（`reports/qa-checks/_out-search-index-and-updates-order.json`）と突き合わせ
4. 重複ID・必須フィールド欠落・不正な日付・未来日付を機械チェック
5. 件数表記（「◯件追加」等）を、該当エントリを実際に追加したコミット時点のデータ差分と突き合わせて検証（抜き取りではなく、直近の「議案・表決」カテゴリ15件全件を検証）
6. `linkUrl`が`src/App.tsx`の実在ルートと一致するか全件確認
7. カテゴリの一貫性・内部QA作業の過大表示の有無を確認

## 発見した問題（5件）

### F1（medium）主要更新の未掲載：commit c0434c7 の市民向け修正2件が記録漏れ

`c0434c7`（2026-08-23 23:06、「トップページ数値不一致の原因究明+恒久監視化、委員会/公約表示の矛盾解消...」）は以下2件の市民向け表示修正を行ったが、`updateHistory.json`への追加は一切ない（`git show c0434c7 -- src/data/updateHistory.json` で差分0を確認）。

- データ収録状況ページ「委員会：所管事項の確認」で、実際は6/6件確認済みなのに古い説明文（一部未確認だった頃の文言）が表示され続けていたバグを修正
- トップページ「登録済み市長公約数（4件）」とデータ収録状況ページ「政策・公約（16件）」の集計対象の違いを説明する注記を追加（集計対象が異なるのに同じ「公約」という言葉で桁違いの数字が並んでいた表示矛盾への対応）

いずれも典型的な「表示改善／品質改善」カテゴリに該当し、他の同種の修正（u92, u96, u100など）と同じ基準で記録すべき内容。追加すべきエントリ案を後述。

### F2（high）件数表記の誤り：u84「25件」→ 実際は33件

`u84`「令和元年9月定例会の議案審議結果を追加しました（25件）」を、実際にこのエントリを追加したコミット `e838a4c`（2026-08-09）の前後でbillVotes.jsonを比較したところ、同セッションの新規追加件数は33件だった（現在のデータでも33件のまま）。25件という数字は議案番号付きレコードのみ（31件）とも、決議・請願を除いた数とも一致せず、単純な集計ミスと考えられる。

### F3（low）件数表記の誤り：u87「24件」→ 実際は25件

同じコミットで追加された `u87`「令和3年6月定例会の議案審議結果を追加しました（24件）」も、実際の追加件数は25件（現在も25件のまま）で1件ずれている。

### F4（medium）u120のページ名表記が実際のページ名・既存エントリと不整合

`u120`（2026-08-24、`6c561dc`で追加）の`targetPages`・`linkLabel`は「歴代市役所特別職一覧」だが、リンク先`/city-officials`の実際のページ見出し（H1）・SEOタイトルはいずれも「副市長・教育長・行政委員会委員」であり、「歴代市役所特別職一覧」という文言はサイト内に存在しない。同じページを参照する既存6件のエントリ（u46, u67, u68, u88, u98, u103）はすべて「副市長・教育長・行政委員会委員」で統一されており、u120だけが異なる呼称になっている。なお`linkUrl`自体（`/city-officials`）は正しく、csp-56/csp-57のデータも同ページの「歴代（元職）」セクションに正しく表示されるため、実害はページ名表記のみ。

### F5（info-only）配列の並び順：既知の課題であり、今回のPhaseで新規に悪化させていない

`reports/qa-checks/_out-search-index-and-updates-order.json`（2026-08-24T06:09:09.730Z生成）で`isDescending: false`, `outOfOrderCount: 7`と報告されている。内訳を確認したところ、配列前半（u97→u1、日付降順で2026-08-17〜2026-07-14）の直後に、配列後半（u98→u123、日付昇順で2026-08-22〜2026-08-24）が単純に追記されている構造であり、`u120`〜`u123`（今回対象の2コミットで追加）は後半ブロックの既存パターンをそのまま踏襲しているだけで、新たな乱れを加えてはいない。

表示への実害は無い。`src/pages/HomePage.tsx`・`UpdatesPage.tsx`はいずれも`sortUpdateHistoryByDateDesc()`（`src/lib/updateHistory.ts`、日付降順・同日は配列順を保つ安定ソート）経由で表示しており、前半ブロックと後半ブロックの間で日付の重複が無いため、画面上の「新しい順」表示は正しく機能する。対応は不要だが、将来の保守性のためPhase118以降で配列を日付降順に一括整理することを低優先度で検討してもよい。

## 確認したが問題なしと判断した項目

- **重複**：完全一致するタイトル・説明文は0件。ID重複も0件（機械チェック、124件すべてユニーク）。
- **未来日付**：今日（2026-08-24）より未来の日付は0件。
- **必須フィールド欠落・不正な日付形式**：0件。
- **linkUrl**：全17件のlinkUrl値（`/`, `/bills/votes`, `/methodology/activity-radar`, `/themes`, `/council-documents`, `/city-guide`, `/petitions`, `/questions`, `/mayors`, `/finance`, `/mayor/press-conferences`, `/history`, `/elections`, `/council-activity`, `/city-officials`, `/compensation`, `/data-status`）すべてが`src/App.tsx`の実在ルートと一致（F4を除きページ名表記も概ね一致）。
- **軽微な内部修正の過剰掲載**：全124件のtitle/descriptionを内部用語（スクリプト・自動検証・CI/CD・validate・lint・typecheck等）で検索したところ、`u47`（「自動検証を追加しました」を含むが、同時に市民向けの内部リンク切れ修正も含む複合エントリ）以外に該当なし。QA監査スクリプト追加のみを大きく見せている記録は確認できなかった。`6c561dc`・`dbdc53a`が自ら「内部QA・sourceRefs補強は市民に意味のある表示変更を伴わないため対象外とした」と判断した項目（Phase99 linkHealth再実測、Phase106 warnings再分類、Phase107 sourceRefs格上げ123件）についても、該当データ（`verificationStatus`等）がページUIで直接表示されていないことを実際にコード検索で確認し、除外判断は妥当と判断した。
- **u101の「26件（請願11件・陳情15件）」**：追加当時のコミット（`3c3af0b`）での新規追加分として正確。現在の累計（請願14件・陳情19件）と一致しないのは、元々ファイルに存在した7件に26件を積み増した結果であり、「新規に◯件追加した」という記述としては問題ない。
- **カテゴリの一貫性**：カテゴリ体系（新規追加／データ更新／表示改善／出典追加／修正／議案・表決／品質改善／新機能）のうち「修正」は0件使用（未使用自体はエラーではない）。「品質改善」と「表示改善」の使い分け基準はやや曖昧だが、明確な誤分類は確認できなかった。

## 追加すべきエントリ案（1件）

`phase116-updatehistory-audit-findings.json`の`proposedNewEntries`に構造化データを格納。概要:

- 日付案: 2026-08-23（`c0434c7`のコミット日）
- タイトル案: 「委員会ページの説明文の誤り、公約件数の表示矛盾を修正しました」
- カテゴリ案: 品質改善
- 対象ページ: データ収録状況、トップページ
- リンク: `/data-status`

## 既存エントリへの修正案（3件）

`phase116-updatehistory-audit-findings.json`の`proposedFixesToExistingEntries`に構造化データを格納。

| エントリ | フィールド | 現状 | 修正案 |
|---|---|---|---|
| u84 | title | 令和元年9月定例会の議案審議結果を追加しました（**25件**） | 令和元年9月定例会の議案審議結果を追加しました（**33件**） |
| u87 | title | 令和3年6月定例会の議案審議結果を追加しました（**24件**） | 令和3年6月定例会の議案審議結果を追加しました（**25件**） |
| u120 | targetPages / linkLabel | 歴代市役所特別職一覧 | 副市長・教育長・行政委員会委員 |

## その他の軽微な所見（対応不要・参考情報）

- `u84`の`date`フィールド（2026-08-08）が、実際にエントリが追加されたコミット日（2026-08-09）と1日ずれている。同時期に追加された他エントリとの日付グルーピングの可能性があり、実害的な誤りとまでは判断していない。
- `src/pages/UpdatesPage.tsx`のコード内コメント「88件（2026-08時点）を1ページに全件表示すると…」は現在124件のため数字が古い。ページネーション自体は`sorted.length`を動的計算しており機能上の問題はない。`updateHistory.json`自体の問題ではないため、参考情報として付記のみ。

## まとめ

- **発見した問題件数**: 5件（F1〜F5。うちF5は「既知の課題で悪化なし」の確認情報）
- **追加すべきエントリ案の件数**: 1件（F1対応）
- **既存エントリの修正案件数**: 3件（F2, F3, F4対応、うち1エントリu120は2フィールド修正）

`src/data/updateHistory.json`は本Phaseでは変更していない。Phase118統合担当は、本レポートおよび`phase116-updatehistory-audit-findings.json`の内容を、他workerの提案と合わせて確認のうえ反映を判断すること。
