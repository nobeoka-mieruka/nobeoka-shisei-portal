# 延岡市政見える化ポータル 開発ロードマップ

このファイルは中長期の開発方針と優先順位を管理します。
日々の具体的な作業は `TASKS.md`、実装状況は `PROJECT_PLAN.md`、開発ルールは `CLAUDE.md` を参照します。

最終更新日：2026-07-21

---

## プロジェクトの目的

延岡市民が、市長、市議会議員、議案、一般質問、財政、報酬、市役所案内などの公開情報を、スマートフォンから分かりやすく確認できるようにする。

---

## 開発原則

- 公式公開資料を優先する
- 政治的中立を保つ（特定の政党、会派、議員、候補者、政治団体を支持・推薦・批判しない）
- 推測、架空データ、独自採点を掲載しない
- 未確認情報は状態と理由を明記する（「確認中」「情報未登録」「公表なし」「算定不可」等）
- 出典、基準日、最終確認日を可能な限り表示する
- スマホ表示を最優先する
- 既存データと既存ページを壊さない
- 新機能より正確性、検索性、更新しやすさを優先する
- 大量データをReactコンポーネントへ直接書かない（`src/data`配下で管理する）

---

## 短期優先機能（2026-07-21登録）

次の4機能を短期の最優先項目とする。これらは`TASKS.md`に既存のTASK-004〜TASK-007・TASK-011〜TASK-014として
すでに個別タスク化されていたため、新規番号は割り振らず、既存タスクへ集約する
（TASK-003・TASK-004という番号自体は既存の別タスク「サイト内横断検索の最終動作確認（DONE）」
「議案賛否データの投入（BLOCKED）」に使用済みのため、重複を避けるためTASK-028として新規登録した）。

1. **議案ごとの賛否データベース**（`/bills/votes`、`/bills/votes/:id`）— 画面・検索・検証ロジックは実装済み。実データ（`billVotes.json`）は公式資料確認まで意図的に空配列のまま（データ投入はTASK-004、BLOCKED）。2026-07-21、画面側の残課題（旧`/bills`スタブとの重複解消、賛否区分の拡張、検索インデックス強化、ダッシュボード集計の参照先修正）をTASK-028で対応済み（DONE）。
2. **一般質問データベース**（`/questions`、`/questions/:id`）— 画面は実装済み、既存14件のデータあり。対象年度拡大はTASK-005、答弁概要追加はTASK-006（いずれもBLOCKED、公式資料待ち）。
3. **市長公約の進捗管理**（`/mayor/policy-progress`）— 画面・進捗履歴の型は実装済み。2026-07-21、TASK-007でキーワード検索、進捗状況／政策分野／根拠資料の有無／関連議案の有無／関連一般質問の有無／確認年度の絞り込み、進捗状況区分の拡張（達成・進行中・一部実施・未着手・方針変更・確認中、色＋文字＋アイコンで識別）、関連議案・関連一般質問へのID参照リンクを実装（DONE）。
4. **財政ダッシュボード**（`/finance`）— 歳入・歳出・基金残高・人口推移は実装済み（延岡市単独データ）。他自治体比較・財政指標追加・市債残高複数年度化はTASK-011〜TASK-014（いずれもBLOCKED、公式資料待ち）。

---

## Phase 1：基盤整備（実装済み）

- ホームページ、議員一覧、議員詳細
- 市長ページ、市長交際費、市長定例記者会見
- SEO基本設定（title/description/canonical/OGP/JSON-LD/サイトマップ/robots.txt）
- Google Analytics・Cloudflare Analytics
- 問い合わせ、情報提供・訂正フォーム
- 編集方針、利用規約
- 市役所案内診断
- 更新履歴

---

## Phase 2：議会活動の見える化（実装中）

実装済み部分：
- 市長公約達成状況一覧（`/mayor/policy-progress`）
- 公約詳細ページ（`/mayor/policy-progress/:id`）
- 議案賛否ページの画面・検索・フィルタ・検証ロジック（`/bills/votes`、`/bills/votes/:id`）※2026-07-21、重複していた旧`/bills`スタブ（`bills.json`／`Bill`型、実データなし）を廃止し`/bills/votes`へリダイレクト統合。賛否区分に「棄権」「確認不能」を追加、検索インデックスに年度・議員名を追加
- 一般質問一覧・詳細ページ、テーマ別・年度別・会派別集計（`/questions`、ダッシュボード）
- 議員ページ・市長ページとの相互連携（一部）

データ整備中の部分：
- 議案賛否の実データ（`billVotes.json`は公式資料確認まで意図的に空配列）
- 一般質問の答弁内容（会議録未公開のため未入力）
- 公約一覧の検索・カテゴリ・進捗状態フィルタ
- ホームページから公約進捗ページへの直接導線

---

## Phase 3：市政のお金の見える化

実装済み（延岡市単独データ）：
- 歳入・歳出、基金残高、人口推移（`/finance`）
- 報酬比較（近隣市4市、`/compensation`）
- 市長交際費

未実装・データ整備中：
- 他自治体との財政指標比較
- 市債残高の複数年度推移（現状は単年度の予算計上額のみ）
- 全国報酬比較・類似団体報酬比較（枠は実装済みだが値は`null`）
- 政治資金データベース
- CSVダウンロード

---

## Phase 4：検索とデータ活用

- サイト内横断検索：実装済み（キーワード一致方式、`/search`）
- 検索ハイライト・検索履歴・検索ロジック改善：実装中（未コミット、`TASKS.md` TASK-001参照）
- サジェスト、複数語検索の精度向上：継続改善
- 関連情報の自動リンク：一部実装
- 自然文検索・AI要約：未実装（現状のキーワード検索とは別タスクとして扱う）
- オープンデータAPI、PDF/CSV出力：未実装

---

## Phase 5：運用自動化

- PDF自動取得：未実装
- Excel取込対象拡大：議員データのみ対応。議案・一般質問・報酬データは未対応
- 更新履歴自動生成：一部自動化済み（`siteUpdate.json`）
- リンク切れ検査：仕組み化されていない
- GitHub Actions：未導入
- Lighthouse・Core Web Vitals計測：未計測
- WCAG簡易チェック：未実施
- Search Consoleを基にした改善：確認タグ設置済み、インデックス状況の定期確認は未実施

---

## 優先順位の決め方

次の順に優先する。

1. エラー、誤情報、表示崩れ、リンク切れ
2. 公式資料が揃っている未反映データ
3. 議案、一般質問、公約データベース
4. 財政、報酬、政治資金
5. 検索、関連リンク、利便性
6. 自動化、AI、新機能

---

## 完了の定義

- 要件を満たす
- 公式資料の出典がある
- 未確認値を推定しない
- 375px、390px、430px、768px、1280pxでのスマホ表示を確認
- `npm run validate:data` 成功
- `npm run typecheck` 成功
- `npm run lint` 成功
- `npm run build` 成功
- `TASKS.md` 更新
- `PROJECT_PLAN.md` 更新
- 必要に応じて `RELEASE_CHECKLIST.md` と更新履歴を更新
- GitHubへコミット、プッシュ
- Cloudflare Pagesのデプロイ確認

---

## データ管理の原則

- データは `src/data` 配下で管理し、大量データをコンポーネントへ直書きしない
- ID重複を防止する
- 日付形式を統一する（`YYYY-MM-DD`）
- URLを検証する
- status値は型（Union型）で管理する
- 出典のない確定値は登録しない
- 未確認データを`0`や架空値として扱わない（`null`または「確認中」等で明示する）
- 自動生成データ（`searchIndex.json`、`sitemap.xml`、`siteUpdate.json`等）と手入力データを区別する
- 関連ID（会派ID、議員ID、公約カテゴリID等）が存在するか検証する
- 金額単位（月額/年額）を統一・明記する
- 割合は0～100の範囲で検証する
- 年度と基準日を区別する
- 更新日と最終確認日を区別する
- 公式資料の転載ではなく、必要な数値と要約のみを整理する
