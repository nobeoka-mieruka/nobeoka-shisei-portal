# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.wrangler
*.local
.env
!.env.example
.dev.vars
!.dev.vars.example

# データ取込のプレビュー出力・バックアップ（Excel/CSV取込機能）
generated/
backups/

# 会議録検索システムから取得した生HTML・正規化テキストのローカルキャッシュ
# （公式会議録の著作物をそのまま複製・再配布しない方針のため、Git管理しない。
# docs/council-speech-summary-pipeline.md 参照）
data/minutes/

# scripts/lib/minutes-source.mjs のHTTP取得結果ローカルキャッシュ（同一URL再取得防止）
scripts/.cache/

# scripts/sync-council-data.mjs の実行状態（5日ごと自動巡回の前回正常実行日時等）。
# GitHub Actions側ではactions/cacheで保持し、Gitへはコミットしない。
scripts/_sync-state.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
